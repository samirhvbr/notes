# Consulta Nota Chave Janela

inventário tarefa inventário índice chave consulta alerta container reunião runbook política projeto nota deploy rotação container índice servidor reunião tarefa rotação chave retenção métrica nota servidor rede janela incidente reunião

deploy projeto servidor projeto métrica backup nota índice rotação backup firewall rede métrica servidor tarefa runbook rotação rotação runbook janela runbook runbook topologia servidor reunião consulta cliente cliente servidor container inventário
