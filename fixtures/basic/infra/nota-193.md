# Tarefa Rotação Inventário Inventário

retenção chave backup consulta janela chave servidor rotação chave chave firewall topologia cliente consulta firewall backup rede rede chave índice container backup topologia consulta chave nota índice chave inventário topologia tarefa rede latência runbook política chave container deploy backup firewall latência métrica alerta cliente reunião reunião tarefa janela chave runbook backup chave topologia métrica tarefa inventário backup backup

deploy consulta nota volume rede container índice firewall backup retenção inventário deploy latência deploy retenção firewall incidente latência volume backup topologia inventário rede volume alerta topologia latência métrica inventário rotação política métrica métrica tarefa inventário rotação cliente latência container firewall rotação rede incidente retenção incidente cliente alerta servidor nota servidor projeto métrica consulta deploy tarefa firewall rotação cliente métrica
