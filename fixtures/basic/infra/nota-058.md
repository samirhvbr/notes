# Chave Servidor Janela Backup

container métrica retenção inventário tarefa backup política servidor alerta inventário nota consulta consulta consulta incidente política chave volume incidente firewall incidente inventário projeto rede rotação reunião volume backup nota servidor topologia rotação cliente tarefa latência tarefa latência tarefa firewall incidente inventário janela volume

nota backup firewall backup nota índice métrica inventário rede volume runbook deploy container consulta servidor consulta tarefa nota projeto política topologia latência projeto cliente chave firewall runbook runbook servidor incidente backup política rede índice firewall cliente deploy topologia política container rede volume projeto firewall

incidente consulta rotação nota topologia firewall incidente chave topologia índice chave firewall cliente alerta cliente política alerta volume retenção inventário topologia container firewall métrica janela métrica tarefa cliente projeto inventário tarefa inventário métrica índice nota backup consulta topologia cliente incidente deploy retenção rotação tarefa consulta
