# Política Firewall Política Rotação

política runbook tarefa runbook container runbook janela firewall firewall runbook métrica chave nota container retenção cliente runbook nota consulta projeto reunião retenção runbook incidente servidor inventário rede runbook

alerta cliente política backup topologia política rede rotação nota rotação projeto runbook rotação runbook nota latência chave projeto consulta container deploy rede rotação backup projeto backup cliente métrica inventário

reunião backup consulta inventário incidente reunião janela consulta cliente container consulta servidor tarefa inventário tarefa latência rede consulta deploy incidente inventário runbook chave topologia rede deploy janela índice rotação container

firewall firewall topologia incidente alerta retenção inventário servidor topologia firewall consulta rede índice métrica rotação backup volume consulta política cliente tarefa servidor firewall política consulta tarefa métrica backup latência política volume
