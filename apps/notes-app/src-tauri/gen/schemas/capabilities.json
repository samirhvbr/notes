{"default":{"identifier":"default","description":"SCOPE §4: window, dialog (folder picker), clipboard and shell-open. NO filesystem. Every read and write goes through a command in this crate, which validates the resolved path against the workspace root itself — a capability is not that validation.","local":true,"windows":["main"],"permissions":["core:default","dialog:allow-open"]}}