// `@testing-library/jest-dom` adds the DOM matchers the menu tests read with —
// `toHaveTextContent`, `toHaveAttribute`. Loaded only where a DOM exists; the
// store tests run in `node` and never see this file's effects.
import "@testing-library/jest-dom/vitest";
